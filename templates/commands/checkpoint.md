# Checkpoint

Create checkpoint before risky changes.

## Methods

- Stash: `git stash push -m "checkpoint-description"`
- Branch: `git branch checkpoint-description`
- Tag: `git tag -a checkpoint-name -m "description"`

## Before Creating

Verify tests pass.

## After Creating

Confirm checkpoint exists, then proceed.

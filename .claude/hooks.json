{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write \"$file_path\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if git branch --show-current 2>/dev/null | grep -qE '^(main|master|production)$'; then if echo \"$command\" | grep -qE '^git (commit|push|reset)'; then echo 'WARNING: Operating on protected branch' >&2; fi; fi; exit 0"
          }
        ]
      }
    ]
  }
}
